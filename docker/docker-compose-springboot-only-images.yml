version: '3.8'

services:
  angular-app:
    image:  
    ports:
      - "4200:4200"
    command: >
      sh -c "ng serve --host 0.0.0.0 --disable-host-check"

  ayurveda-server:
    image: ayurveda-server
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: "prod"
